{
  "name": "typescript-graphql-schemas",
  "private": true,
  "description": "Comparison of TypeScript GraphQL schema builders",
  "scripts": {
    "format": "prettier --write .",
    "dev:gqloom": "pnpm -F gqloom dev",
    "print:gqloom": "pnpm -F gqloom print"
  },
  "devDependencies": {
    "@types/node": "^25.0.3",
    "prettier": "^3.4.2",
    "typescript": "^5.7.2"
  }
}
